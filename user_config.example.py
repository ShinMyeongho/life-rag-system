"""
사용자 프로필 설정 예시 파일.

사용 방법:
  1. 이 파일을 'user_config.py'로 복사
  2. 내용을 본인 정보로 수정
  3. user_config.py는 .gitignore에 등록되어 있어 git에 업로드되지 않음

  cp user_config.example.py user_config.py
"""

USER_PROFILE = {
    # 기본 정보
    "name": "홍길동",
    "age": 30,

    # 현재 관리 중인 목표 (Claude 분석 리포트에 반영)
    "goals": [
        "수면 7시간 이상 유지",
        "주 3회 이상 운동",
        "월 저축 목표 달성",
    ],

    # 현재 상황 및 성향 (분석 컨텍스트로 활용)
    "about": """- 데이터/AI 분야 직무 전환 준비 중
- 혼자 모든 걸 해결하려는 경향이 있어 과부하 주의 필요
- 수면 불규칙이 업무 집중력에 직접 영향을 주는 패턴 있음
- 재정적으로 대출 상환 중, 지출 관리가 중요한 시기""",

    # Claude 분석 리포트 생성 시 적용할 규칙
    "analysis_rules": """- 단기(3일 미만) 데이터로 성격이나 상태를 단정짓지 마
- 위험 요소만큼 안정 요소와 회복 자원도 함께 언급해줘
- 구체적인 수치(수면 시간, 스트레스 점수)를 근거로 사용해
- 실행 가능한 제안을 1~2개로 압축해서 제시해줘""",
}
